# =============================================================================
# Media Gallery Downloader - Docker Deployment Configuration
# =============================================================================
# These settings configure your Docker deployment (ports, paths, timezone).
# Copy this file to .env and customize as needed.
#
# Note: Application settings (Laravel config) are baked into the Docker image.
# This file only contains deployment-specific overrides.
# For development, see .env.development

# =============================================================================
# Data Storage
# =============================================================================
# Path for all persistent data (mounted to /app/storage in container)
# Contains: database, media files, thumbnails, backups, logs, cache
# DATA_PATH=./storage

# =============================================================================
# Network
# =============================================================================
# HTTP port for the application
# HTTP_PORT=8080

# =============================================================================
# System
# =============================================================================
# Timezone (e.g., America/New_York, Europe/London, Asia/Tokyo)
TZ=UTC

# Container memory limits
# MEMORY_LIMIT=4G
# MEMORY_RESERVATION=1G

# =============================================================================
# Bulk Import (Optional)
# =============================================================================
# Number of files to queue per batch (default: 10)
# MGD_IMPORT_BATCH_SIZE=10
