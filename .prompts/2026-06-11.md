# Prompts - June 11, 2026

---

### Prompt 1

**Model:** Claude Opus 4.8

**Context:** Part of a larger multi-stack home-server session (arr/caddy/plex/immich). mgd was moved from `php/` into `docker/`. This entry covers the mgd-relevant asks: the CLAUDE.md reorg and the Caddy reverse-proxy entries for two mgd instances.

> I've moved the media-gallery-downloader (mgd) app into the docker directory as well. Update CLAUDE.md to reflect the current organization of the workspace directory.
>
> update the caddy stack so that it incorporates 2 separate entries for the mgd app- one for mgd and one for a second instance mgd-xxx.
>
> scaffold out a new stack in the docker/ directory for immich with an eye towards security, stability, durability, and performance.
>
> recommend a backup process and system for these for both appdata and actual data. prefer aws and backblaze and try to evaluate costs for different options

---

### Prompt 2

**Model:** Claude Opus 4.8

**Context:** After the Caddy `conf.d/mgd.caddy` entries were added (mgd + mgd-xxx). This wires the mgd compose onto the shared `edge` network so the Caddy routes resolve: `app` gets a per-instance `edge` alias (`EDGE_ALIAS`, default `mgd`) and the host port is bound to `127.0.0.1`. Also initializing git for the new stacks.

> yes update the mgd compose and git init everything
