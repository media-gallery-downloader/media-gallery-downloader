# Prompts - June 12, 2026

---

### Prompt 1

**Model:** Claude Opus 4.8

**Context:** The app was moved from `docker/` back to `php/media-gallery-downloader`. Earlier (while it lived in `docker/`) the project's `docker-compose.yml` had been edited to join a homelab-specific external `edge` network with a per-instance alias. This entry covers reverting those homelab edits so the shipped/generic compose works for any user again; the homelab two-instance deployment now lives in separate `docker/mgd` and `docker/mgd-xxx` stacks.

> ok, I've moved media-gallery-downloader back to the php/ directory. create the mgd and mgd-xxx stacks and update everything else including documentation accordingly. the data paths should be volume1/data/mgd and volume1/data/mgd-xxx unless you have a better idea. they should be in their own repos
>
> also check to see whether anything in the media-gallery-downloader needs to be updated regarding this change
>
> once this is all set up and running, I'd like the repos for all the docker stuff to be pushed to the new forgejo instance. can you make sure the git remotes are set appropriately for that? obviously it won't work until I've started the stacks, but once they are up and running and configured, I'd like it to work.
>
> can they also be automatically synced to github/gitlab private repos for redundancy?
